This directory contains simple tests for the CVC5 preprocessing pass implementation. \
.smt2 files are testing inputs; \
.exp files are manually inspected expected outputs; \
Run `test.sh <total/partial>` to generate preprocessing pass output (.out files) and the testing result.
